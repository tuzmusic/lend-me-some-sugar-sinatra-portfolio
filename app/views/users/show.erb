<h1><%= @user.username %>
 <% if @user.id == @current_user.id %>(You)<% end %>
        </h1>

<% if @user.id == @current_user.id %>Your
<% else %> <%= @user.username %>'s 
<% end %>
ingredients:
<ul>
  <% if @user.ingredients.empty? %>
    <li><%= @user.username %> hasn't listed any ingredients. 😢</li>
  <% end %>
  <% @user.ingredients.each do |ingredient| %>
    <li><%= ingredient.name %></li>
  <% end %>
</ul>

<a href="/index">Home</a>